import 'package:flutter/material.dart';

class LayoutUtils {
  static const String fontFamily = 'LexendDeca';

  /// layout padding
  static const layoutPadding = EdgeInsets.only(top: 20.0, right: 15.0, left: 15.0, bottom: 10.0);

  static const primaryTextColor = Color(0xff2f2f2f);
  static const accentTextColor = Color(0xff535353);
}
